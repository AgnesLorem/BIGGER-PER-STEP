# MVP-005 — WORLD1 Reward Choice and AFK Zones

## Status

```text
MVP005_PLANNING: APPROVED
PAID_PORTAL_PRODUCT: ONE PRODUCT AT 29 ROBUX
VIP_DISCOUNTED_PRODUCT: REMOVED
GAMEPASS_QUERY_RACE_PROTECTION: REQUIRED
RECEIPT_WORLD_CORRELATION: REQUIRED
SOURCE_IMPLEMENTATION: AUTHORIZED AFTER EXACT PATH SCOPE IS RECORDED
MVP005_CUSTOM_THUMBNAILS: DEFERRED
DEFAULT_ROBLOX_ASSET_IMAGES: APPROVED
CUSTOM_THUMBNAILS_BLOCKING: NO
REAL_MONETIZATION_IDS: STILL_REQUIRED
PRODUCTION_PUBLICATION: NOT_AUTHORIZED
```

## Objective

MVP-005 completes the WORLD1 objective loop with a three-second objective popup,
server-authoritative Spider stomp validation, free or paid reward-portal selection,
durable Developer Product delivery, VIP and Premium AFK Zone Game Pass ownership,
seven deterministic AFK zones, and fractional growth accumulation.

The player remains `InWorld` after the Spider is stomped. Stomping activates the
reward choices but grants no Destruction. The free portal grants exactly one
Destruction and returns immediately. The paid portal uses one repeatable,
portal-only Developer Product priced at 29 Robux and grants exactly three
Destruction only through the durable receipt path.

## Monetization Preflight

Create and record real IDs before adding runtime configuration:

- VIP Game Pass — 199 Robux.
- Premium AFK Zone Game Pass — 99 Robux.
- `World1TripleReward` Developer Product — 29 Robux, unlisted, managed and
  optimized pricing disabled, `Surface = "PortalOnly"`, repeatable.

Custom monetization thumbnails are deferred to a future visual-polish milestone.
MVP-005 uses Roblox default asset imagery temporarily. Missing custom thumbnails
do not block gameplay implementation, Studio verification, code review, or
MVP-005 completion. This correction is user-approved. Existing generated images
remain local future references and are outside repository scope; custom artwork
is not complete. Fake, temporary, or placeholder Asset IDs are prohibited. Real
private-staging purchase QA and production publication are outside this task.

## Authoritative Contracts

### Developer Product

```text
Product key: World1TripleReward
Price: 29 Robux
Reward: exactly 3 Destruction
Surface: PortalOnly
Repeatable: true
```

Existing Bigger products remain `Surface = "Shop"`. Only Shop products require
matching `ShopUIConfig` entries. Product IDs and AnalyticsKeys are globally unique,
every product has exactly one supported reward, and invalid configuration fails boot.

The only paid grant path is:

```text
ProcessReceipt
→ RewardService
→ RewardDispatcher
→ DestructionService:GrantDestruction
→ SaveService:CommitTransaction
→ DeveloperProductCommitted(Player, ProductKey, PurchaseId)
```

`RewardPortalService` never grants the paid reward. Duplicate durable receipts
return `PurchaseGranted` without granting or publishing again.

### Reward Choice State

```text
Inactive → Choosing → PromptOpen → AwaitingReceipt → Settled
                     ↘ Choosing when cancelled or not purchased
```

Free choice is `Choosing → Settled → Returning → InLobby`. Only the private-world
owner can use either portal. Each world owns at most one active prompt.

Runtime prompt correlation is:

```luau
PendingRewardPrompt = {
	WorldInstanceId: string,
	ProductKey: string,
	ProductId: number,
	PromptToken: string,
	State: "PromptOpen" | "AwaitingReceipt",
}
```

A committed receipt settles the current world only when the Player, authoritative
session, writable profile, owner, WorldInstanceId, ProductKey, ProductId,
PromptToken, and `AwaitingReceipt` state still match. Delayed receipts still grant
and save exactly once, but never settle or teleport an unrelated or newer world.
This correlation is intentionally runtime best-effort; MVP-005 does not add a
durable purchase-intent model.

### Game Passes

`GamePassConfig` contains exactly `Vip` and `PremiumZone`. Every ownership query
uses a per-pass generation token. A result applies only while its token, player,
session, writable profile, PassKey, and configured Asset ID remain current.
Successful true/false results replace the saved entitlement, API failure preserves
the last saved value, and changed results queue one save. Purchase-finished events
only start a fresh query.

VIP retains its tag and global growth x2. Premium Zone Pass only unlocks
`AFKZone7`; it adds no multiplier beyond that zone's 4.5x.

### Destruction

`DestructionService:GrantDestruction(Player, Amount): boolean` is the only gameplay
mutation and presentation-sync path. It accepts only positive safe integers,
rejects frozen/missing sessions and overflow, mutates through `Sessions.Mutate`,
updates the Player attribute and leaderstats, and does not save, teleport, process
receipts, or change world state.

### Objective Popup and Portals

The client shows exactly `STOMP THE SPIDER` at full opacity, starts fading at 2.5
seconds, and is fully hidden at 3.0 seconds. A local generation token and cancelled
or superseded tween prevent an old popup from hiding a newer one.

`FreeRewardPortal` and `PaidRewardPortal` live directly under
`ServerStorage.WorldTemplates.WORLD1` with exact `Visual`, `Trigger`, and
`LabelPart.SurfaceGui.TextLabel` paths. They begin hidden and inactive. Free uses
green with `FREE • 1 DESTRUCTION`; paid uses gold/purple with
`3 DESTRUCTION • 29 R$`. No VIP price appears.

### AFK Zones and Fractional Growth

Priority is `AFKZone7` through `AFKZone1`; the highest containing zone wins. A
locked AFKZone7 grants zero and never falls back to an overlapping free zone.

| Zone | Multiplier | Access |
| --- | ---: | --- |
| AFKZone1 | 1x | Free |
| AFKZone2 | 2x | Free |
| AFKZone3 | 2.5x | Free |
| AFKZone4 | 3x | Free |
| AFKZone5 | 3.5x | Free |
| AFKZone6 | 4x | Free |
| AFKZone7 | 4.5x | Premium Zone Pass |

Premium Zone prompts once per entry and can prompt again only after exit/re-entry.
Verified ownership begins granting 4.5x on the next tick even without leaving.

`GrowthFormula.Calculate` returns unfloored gain. `GrowthService` combines it with
runtime-only `Session.GrowthRemainder`, applies the whole Bigger gain first, and
updates the remainder only after a successful mutation. Failed, frozen, or
overflowing mutations do not consume remainder. Expected sequences are 2.5x →
`2, 3, 2, 3`, 4.5x → `4, 5, 4, 5`, and VIP × 4.5x → `9` each tick.

## Exact Repository Scope

### Create

- `tasks/MVP-005.md`
- `docs/adr/ADR-005_reward_portal_selection.md`
- `docs/superpowers/specs/2026-07-18-mvp-005-gameplay-loop-design.md`
- `docs/superpowers/plans/2026-07-18-mvp-005-gameplay-loop.md`
- `docs/superpowers/reports/mvp-003/2026-07-18-mvp-005-audit.md`
- `src/server/Core/Config/GamePassConfig.luau`
- `src/server/Core/Services/GamePassService.luau`
- `src/server/Game/Services/RewardPortalService.luau`
- `tests/unit/mvp005_gameplay.luau`
- `tests/studio/mvp005_gameplay.luau`

### Modify

- `tasks/MVP-003.md`
- `docs/GAME.md`
- `docs/RUNTIME.md`
- `docs/SAVE.md`
- `docs/TASKS.md`
- `docs/CHANGELOG.md`
- `docs/examples/PlayerProfileExample.md`
- `docs/superpowers/reports/mvp-003/README.md`
- `src/client/controllers/GuiController.luau`
- `src/client/controllers/ObjectiveController.luau`
- `src/server/Core/Bootstrap/CoreBootstrap.luau`
- `src/server/Core/Config/DeveloperProductConfig.luau`
- `src/server/Core/Registry/RuntimeRegistry/Sessions.luau`
- `src/server/Core/Registry/RuntimeRegistry/WorldInstances.luau`
- `src/server/Core/Services/ConfigValidationService.luau`
- `src/server/Core/Services/GrowthService.luau`
- `src/server/Core/Services/ReceiptProcessingService.luau`
- `src/server/Core/Services/SaveService.luau`
- `src/server/Core/Services/SessionService.luau`
- `src/server/Core/Utilities/PlayerProfileSchema.luau`
- `src/server/Core/Utilities/RewardDispatcher.luau`
- `src/server/Game/Config/AFKZones.luau`
- `src/server/Game/Config/World1Config.luau`
- `src/server/Game/Formula/GrowthFormula.luau`
- `src/server/Game/GameBootstrap.luau`
- `src/server/Game/Services/DestructionService.luau`
- `src/server/Game/Services/WorldInstanceService.luau`
- `src/server/Game/Systems/AFKZoneSystem.luau`
- `src/shared/Core/Config/ShopUIConfig.luau`
- `src/shared/Core/Enums/RewardType.luau`
- `tests/unit/save_system.luau`

Any additional repository path requires a documented dependency, exact-scope
amendment in this task and the implementation plan, and user review before commit.

## Studio Scope

- Remove the approved orphan `Workspace.AFKZones.Spawn` near
  `(-803, 937, -18049)`.
- Rename the three lobby triggers and create `AFKZone1` through `AFKZone7`.
- Create `FreeRewardPortal` and `PaidRewardPortal` under the WORLD1 template.
- Create/verify Studio source mirrors for the three new ModuleScripts required by
  Jarvis/Rojo parity; this is source mapping, not additional gameplay asset scope.

No other GUI, map, persistence, gameplay, or monetization asset may change.

## Verification and Release Decision

Required offline commands:

```powershell
lune run tests/unit/movement_validation.luau
lune run tests/unit/stomp_validation.luau
lune run tests/unit/save_system.luau
lune run tests/unit/mvp005_gameplay.luau
stylua --check src tests
selene src tests
rojo build default.project.json -o "$env:TEMP\bigger-mvp005.rbxl"
git diff --check
```

Studio verification uses a synthetic receipt harness and must label the evidence
synthetic. It covers normal, duplicate, delayed, wrong-product, post-cleanup, and
post-disconnect receipts plus matching and mismatching portal settlement. Real
purchase evidence requires separately authorized private staging.

The maximum task status without real monetization QA is:

```text
MVP005_GAMEPLAY: READY_FOR_REVIEW
REAL_MONETIZATION_QA: DEFERRED
PRODUCTION_PUBLICATION: NOT_PERFORMED
```

Do not push, merge, or publish production without explicit user review.
