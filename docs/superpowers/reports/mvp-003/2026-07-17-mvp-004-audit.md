# MVP-003 Audit Cycle: MVP-004

## Scope

MVP-004 covers native DataStore persistence, profile validation, per-UserId write serialization, leases, mutation freezing, autosave/final/shutdown writes, offline progression, durable Developer Product receipts, and later verified Game Pass ownership.

The exact allowed paths are recorded in `tasks/MVP-004.md` and the `Audit Cycle: MVP-004` section of `tasks/MVP-003.md`. Any additional path requires Tech Lead approval before editing.

## Baseline and Merge Evidence

```text
MVP-003 feature tip: c5e34f8
MVP-003 PR: https://github.com/AgnesLorem/BIGGER-PER-STEP/pull/1
MVP-003 merge method: normal merge commit
MVP-003 merge commit: 5367ecd
MVP-004 branch: feat/mvp-004-data-store
MVP-004 branch base: origin/main at 5367ecd
```

The merge gate supports normal ancestry and equivalent squash/rebase verification. This cycle used normal ancestry: `c5e34f8` is an ancestor of `5367ecd`.

## Approved Contracts

- DataStore: `BiggerPlayerProfiles_v1` with `Production`, `Staging`, and `Studio` scopes and `Player_<UserId>` keys.
- Production PlaceId: `104031194350622`.
- Stored processed receipt ceiling: 5,000.
- Runtime durable-plus-pending receipt ceiling: 5,000.
- Pending receipt IDs are runtime-only and are never stored as a separate profile field.
- Session lease: 180 seconds.
- Autosave: 60 seconds.
- Shutdown deadline: 25 seconds.
- Existing `SaveService` public signatures remain unchanged.
- Non-monetization source work is approved within exact scope.

Exact validation limits:

```text
Maximum unlocked Growth Upgrades: 64
Maximum completed portals: 256
Maximum stored processed receipts: 5,000
Maximum durable-plus-pending receipts at runtime: 5,000
Maximum content ID length: 64
Maximum PurchaseId length: 128
Maximum safe integer: 9007199254740991
```

## External Blockers

```text
Private staging PlaceId: not available
Authorized Game Pass thumbnail source: not available
2X Multiplier Game Pass Asset ID: not available
VIP Pass Game Pass Asset ID: not available
```

Planning and non-monetization persistence may proceed. Game Pass config, staging environment mapping, ownership-query implementation, Game Pass shop entries, Game Pass prompting, and staging verification remain blocked. No temporary identifier is permitted.

## Findings

No unresolved Critical, High, Medium, or Low finding remains in the implemented non-monetization persistence scope.

The existing entitlement Developer Product prototypes still contain `999991` and `999992`. They were not converted, reused, or treated as legitimate IDs because Task 8 is externally blocked. Monetization must remain disabled until those prototypes can be replaced by real Game Pass configuration and verified in private staging.

## Verification Record

Implementation commits:

```text
Planning: 4decf5c
Schema/config: f513f61
FIFO registry: e9c2c84
Load/lifecycle: d3dbeec
Mutation freeze: ca7aaf3
Offline progression: 7281efc
Durable receipts: 5a7242a
Shutdown finalization: 422c9ae
```

Final offline verification on 2026-07-17:

```powershell
lune run tests/unit/movement_validation.luau
lune run tests/unit/stomp_validation.luau
lune run tests/unit/save_system.luau
stylua --check src tests
selene src
rojo build default.project.json -o "$env:TEMP\bigger-mvp004-final.rbxl"
git diff --check
git status --short
```

Results:

```text
movement_validation: PASS
stomp_validation: PASS
save_system: PASS
StyLua: exit 0
Selene: 0 errors, 0 warnings, 0 parse errors
Rojo: built bigger-mvp004-final.rbxl, exit 0
git diff --check: exit 0
```

The save-system suite covers schema limits, environment resolution, per-UserId FIFO behavior, load/final cleanup, leases, ownership loss, complete mutation freeze, timestamp and offline-reward boundaries, durable/pending receipt behavior, ambiguous writes, capacity, concurrent shutdown, the 25-second deadline, diagnostics, release idempotency, and receipt-before-final FIFO ordering.

## Studio Evidence

- Connected place: `Bigger Per Step`, production PlaceId `104031194350622`; all test DataStore access resolved to the isolated `Studio` scope.
- Local-to-Studio normalized source parity passed for every production module changed during Tasks 2 through 7.
- Temporary harness parity passed at `ServerStorage.MVP004Tests.SaveSystem`.
- Five Play Solo stages passed with a real Player: defaults; autosave; restoration plus mutation freeze; exact-once offline claim; final save plus lease release.
- Immediate restart after shutdown reacquired the profile without foreign-lock retries.
- Temporary Studio harness/runner objects and dedicated `MVP004_Harness_*` records were removed after testing.
- No persistence error or unresolved shutdown write appeared. Output retained the pre-existing unrelated `GuiController` infinite-yield warning at line 106 while waiting for `MainHUD.LeftSidebar`; therefore Output was not warning-free.
- Production was not published.

Private staging, multiplayer, and real purchase tests were not executed because the external monetization gate is closed.

Draft pull request: `https://github.com/AgnesLorem/BIGGER-PER-STEP/pull/2`.

## Remaining Risks

- Roblox DataStore failures can outlive the bounded retry window; frozen release state and lease expiry prevent unsafe cross-server overwrite but cannot guarantee a final backend write during a platform outage.
- The staging and Game Pass portion remains externally blocked, and the existing entitlement Developer Product prototype IDs are not valid release configuration.
- Play Solo Output still contains the pre-existing `GuiController`/`LeftSidebar` infinite-yield warning unrelated to persistence.
- `Bigger` remains the compatibility field until a separate approved migration.

## Release Decision

```text
MVP004_NON_MONETIZATION: READY
MONETIZATION: BLOCKED PENDING PRIVATE STAGING PLACE,
AUTHORIZED THUMBNAIL SOURCE, 2X MULTIPLIER GAME PASS ASSET ID,
VIP GAME PASS ASSET ID, PRIVATE-STAGING QA, AND REAL PURCHASE QA
```

The draft PR must not be merged as a complete MVP-004 monetization release until the external gate clears and the missing verification is recorded.
